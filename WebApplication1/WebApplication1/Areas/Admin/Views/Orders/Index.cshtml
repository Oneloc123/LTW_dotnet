@* @using WebApplication1.Models

@model IEnumerable<Orders>
@{
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Quản lý đơn hàng</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Mã</th>
            <th>Ngày</th>
            <th>Tổng</th>
            <th>Trạng thái</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var o in Model)
        {
            <tr>
                <td>#@o.Id</td>
                <td>@o.CreatedAt.ToString("dd/MM/yyyy")</td>
                <td>@o.TotalAmount.ToString("N0") đ</td>
                <td>@o.Status</td>
                <td>
                    <a asp-action="Details" asp-route-id="@o.Id" class="btn btn-sm btn-primary">Chi tiết</a>
                </td>
            </tr>
        }
    </tbody>
</table> *@



<!-- Orders và OrderItem chưa có nên chưa triển khai, mới xây khung -->